============
Installation
============

At the command line::

    $ easy_install python-updict

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python-updict
    $ pip install python-updict
